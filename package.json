{
  "name": "luckyx-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:gorli": "npx hardhat run scripts/deploy.js --network gorli",
    "deploy:rop": "npx hardhat run scripts/deploy.js --network rop",
    "crt": "npx hardhat crt --network localhost",
    "buy": "npx hardhat buy --network localhost",
    "crtnumbers": "npx hardhat numbers --network localhost",
    "rewards": "npx hardhat rewards --network localhost"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@chainlink/contracts": "^0.2.1",
    "@openzeppelin/contracts": "^4.2.0",
    "bignumber.js": "9.0.1",
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.4",
    "hardhat": "^2.6.0"
  }
}
